{
  "version": "0.2.0",
  "configurations": [
    {
      "preLaunchTask": "rust: cargo build",
      "name": "Debug",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/bullet.exe",
      "cwd": "${workspaceRoot}",
      "console": "integratedTerminal"
    }
  ]
}
